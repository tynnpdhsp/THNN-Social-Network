[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = session
markers =
    unit: fast unit tests, no real DB / Redis / SMTP
    integration: HTTP + Prisma integration tests (needs running services)
testpaths = tests
